<div class="products">

    <div class="grid">

        {% for product in products %}

            <a href="{{ path('app_product_show', {'id':  product.id}) }}" class="product {{product.available ? "" : "unavailable" }}">
                <div class="img">
                    <img src="{{ product.imgUrl }}" onerror="this.onerror=null; this.src = 'https://images.unsplash.com/photo-1544947950-fa07a98d237f?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxzZWFyY2h8MTB8fGJvb2t8ZW58MHx8MHx8&auto=format&fit=crop&w=500&q=60'"/>
                </div>
                <h3> {{ product.name }}</h3>
                <p>{{ product.price }} €</p>
            </a>

        {% else %}

            <p >No product records found, wait for the administrator to do his job please :).</p>


        {% endfor %}
    </div>

</div>